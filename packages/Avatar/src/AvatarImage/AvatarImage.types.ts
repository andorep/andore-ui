import { PropsWithChildren } from 'react';
import type { AvatarImageProps as RadixAvatarImageProps } from "@radix-ui/react-avatar";

export interface AvatarImageProps extends PropsWithChildren<RadixAvatarImageProps> {
 }