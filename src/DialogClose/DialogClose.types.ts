import { PropsWithChildren } from 'react';
import type { DialogCloseProps as RadixDialogCloseProps } from "@radix-ui/react-dialog";

export interface DialogCloseProps extends PropsWithChildren<RadixDialogCloseProps> {
 }