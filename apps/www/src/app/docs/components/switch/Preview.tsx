"use client";

import React from "react";
import { Switch, SwitchThumb } from "@andore-ui/switch";

const Preview = () => {
  return (
    <Switch>
      <SwitchThumb />
    </Switch>
  );
};

export default Preview;
