'use client';

import React from 'react';
import { Tab as AndoreTab, TabProps } from "@andore-ui/tabs";


const Tab = (props: TabProps) => {
    return (
        <AndoreTab {...props} />
    );
};

export default Tab;
